#include <data_type.h>

class Solution {
public:
	Solution() {
	}
	bool isMatch(string s, string p);

private:
	virtual ~Solution() {
	}
	Solution &operator=(const Solution &source);
	Solution(const Solution &);
};
