#include <data_type.h>

class Solution {
public:
	Solution() {
	}
	int longestDecomposition(string text);

private:
	virtual ~Solution() {
	}
	Solution &operator=(const Solution &source);
	Solution(const Solution &);
};
