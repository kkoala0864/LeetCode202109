#include <data_type.h>
#include <string>

using namespace std;

class Solution {
public:
	Solution() {
	}
	string stringHash(string s, int k);

private:
	virtual ~Solution() {
	}
	Solution &operator=(const Solution &source);
	Solution(const Solution &);
};
