#include <data_type.h>

using namespace std;

class Solution {
public:
	Solution() {
	}
	vector<int> buildArray(vector<int>& nums);

private:
	virtual ~Solution() {
	}
	Solution &operator=(const Solution &source);
	Solution(const Solution &);
};
